

import './notify.css'
export const Notify = (props)=>{
return (
<div   className="notifying ">
<span>{props.productName + "XX99MARK II HEADPHONES added to cart"}</span>
</div>

)
}
export default Notify;